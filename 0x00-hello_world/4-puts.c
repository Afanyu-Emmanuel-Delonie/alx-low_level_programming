#include <stdio.h>
/**
 * main: a program that print a line with a put function
 * Return: 0 (when the program return 0 then the program was a succes)
 */
int main(void)
{
puts("/"Programming is like building a multilingual puzzle");
return (0);
}

